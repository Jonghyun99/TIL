class PlayfairAlgorithm {

    fun encrypt(plainText:String, key:Array<Array<Char?>>){
        var plainText= ""
        if(plainText.length%2==1){
            plainText += "X"
        }
        var encryptText = arrayListOf<Char>() // 암호화 결과 담을 변수
        var cnt = 0 // for문 2개씩 끊기 위한 flag 변수
        for(i in plainText){

        }
    }
}